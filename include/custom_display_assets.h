/* ──────────────────────────────────────────────────────────────────────────
 * custom_display_assets.h — STUB / placeholder for zmk-display-core.
 *
 * The REAL generated file is produced by zmk-display-builder and must be
 * committed to your zmk-config/config/ directory.  The CMakeLists.txt in
 * zmk-display-core adds the ZMK_CONFIG paths to the compiler include search
 * path BEFORE this directory, so the generated header takes priority.
 *
 * If compilation reaches the #error below, the generated file was NOT found.
 * Fix by exporting from zmk-display-builder and committing the result to
 * zmk-config/config/custom_display_assets.h, then rebuilding.
 * ──────────────────────────────────────────────────────────────────────────
 */
#pragma once

#ifndef HAS_CUSTOM_LAYOUT_BLOCKS
#  error "custom_display_assets.h: generated file not found. Export your layout from zmk-display-builder and commit config/custom_display_assets.h to zmk-config."
#endif
